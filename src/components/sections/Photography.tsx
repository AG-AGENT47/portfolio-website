// v2 — Photography section
// To activate: add `photos` field to PortfolioData in lib/types.ts,
// seed photo records in portfolio-store, fetch them in lib/db.ts,
// then uncomment <Photography photos={data.photos} /> in app/page.tsx.
//
// Expected data shape:
//   photos: Array<{ id: string; src: string; caption: string; display_order: number }>

export function Photography() {
  return null;
}
